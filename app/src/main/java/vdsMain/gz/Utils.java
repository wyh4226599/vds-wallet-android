package vdsMain.gz;

import com.google.common.primitives.UnsignedBytes;

//gz
public class Utils {
    public static short m11590a(byte b) {
        return (short) (b & UnsignedBytes.MAX_VALUE);
    }
}
